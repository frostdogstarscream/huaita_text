# 百度云智能抠图 API 接入说明

## 1. 文档目的

本文档仅说明本项目中 **百度智能云智能抠图接口** 的接入方式。

该模块用于完成：

```text
本地拍照图片
    ↓
读取图片并转为 base64
    ↓
调用百度云智能抠图接口
    ↓
获取透明背景人物 PNG 或 mask
    ↓
保存到本地
    ↓
交给后续 Python 背景合成模块使用
```

本文档不包含摄像头、激光测距、标语轮播、EXE 打包、HTML 页面展示等其他模块。

---

## 2. 接口概述

百度智能抠图接口地址：

```text
https://aip.baidubce.com/rest/2.0/image-process/v1/segment
```

调用方式：

```text
POST
Content-Type: application/json
```

鉴权方式：

```text
access_token
```

请求 URL 示例：

```text
https://aip.baidubce.com/rest/2.0/image-process/v1/segment?access_token=ACCESS_TOKEN
```

本项目推荐使用本地图片 base64 方式调用，不需要先上传 OSS。

```text
本地照片
    ↓
base64 编码
    ↓
调用百度智能抠图
    ↓
返回结果图 base64
    ↓
解码保存为 PNG
```

---

## 3. 依赖安装

```bash
pip install requests pillow
```

说明：

| 依赖 | 用途 |
|---|---|
| `requests` | 获取 access_token 和调用智能抠图接口 |
| `pillow` | 后续读取透明 PNG、换背景、处理 mask |

---

## 4. 你当前示例代码需要补充的关键点

你提供的示例代码中，payload 只有：

```json
{
  "refine_mask": "true",
  "method": "auto"
}
```

实际调用时还需要提供图片内容，建议增加：

```json
{
  "image": "图片base64编码",
  "return_form": "rgba"
}
```

完整请求体建议为：

```json
{
  "image": "图片base64编码",
  "method": "auto",
  "refine_mask": "true",
  "return_form": "rgba"
}
```

其中：

```text
image：本地图片的 base64 编码，不包含 data:image/jpg;base64, 这类前缀
method：auto 表示自动识别主体
refine_mask：true 表示对边缘进行平滑处理
return_form：rgba 表示返回透明背景四通道 PNG
```

---

## 5. 请求参数说明

| 参数 | 是否必填 | 推荐值 | 说明 |
|---|---|---|---|
| `image` | 与 `url` 二选一 | 本地图片 base64 | 推荐本项目使用，避免上传 OSS |
| `url` | 与 `image` 二选一 | 不推荐第一版使用 | 图片公网 URL |
| `method` | 是 | `auto` | 自动识别主体并抠图 |
| `refine_mask` | 否 | `true` | 对边缘进行平滑处理 |
| `return_form` | 否 | `rgba` | 返回透明背景四通道 PNG |

---

## 6. return_form 参数选择

| return_form | 返回结果 | 是否推荐 | 说明 |
|---|---|---|---|
| `rgba` | 透明背景四通道 PNG 的 base64 | 推荐 | 最适合后续 Python 换背景 |
| `mask` | 单通道 mask 图的 base64 | 可选 | 需要结合原图再生成透明人物图 |

本项目建议使用：

```python
return_form = "rgba"
```

原因：

```text
1. 返回结果已经是透明背景 PNG
2. Python 可以直接使用 Pillow 合成背景
3. 不需要再根据 mask 处理 alpha 通道
4. 工程实现最简单
```

---

## 7. AK / SK 配置方式

## 7.1 正式项目推荐方式

正式项目建议使用环境变量：

```text
BAIDU_API_KEY
BAIDU_SECRET_KEY
```

代码读取：

```python
import os

API_KEY = os.getenv("BAIDU_API_KEY")
SECRET_KEY = os.getenv("BAIDU_SECRET_KEY")
```

## 7.2 短期演示项目写法

如果该项目只是短期演示、本地运行、不上传代码仓库、不对外分发，可以将 AK / SK 临时写入后端 Python 代码中。

建议单独创建：

```text
app/secrets.py
```

示例：

```python
# app/secrets.py

BAIDU_API_KEY = "请替换为你的百度云 API Key"
BAIDU_SECRET_KEY = "请替换为你的百度云 Secret Key"
```

然后在服务代码中引用：

```python
from app.secrets import BAIDU_API_KEY, BAIDU_SECRET_KEY
```

注意：

```text
1. 不要把 AK / SK 写入前端 HTML / JavaScript
2. 不要提交到 Git 仓库
3. 不要发给无关人员
4. 建议使用演示专用应用的 AK / SK
5. 演示结束后及时删除或禁用
```

---

## 8. config.json 配置建议

如果需要把百度云接口参数配置化，可以加入：

```json
{
  "matting_api": {
    "provider": "baidu_segment",
    "token_url": "https://aip.baidubce.com/oauth/2.0/token",
    "segment_url": "https://aip.baidubce.com/rest/2.0/image-process/v1/segment",
    "method": "auto",
    "refine_mask": "true",
    "return_form": "rgba",
    "timeout_seconds": 60,
    "retry_times": 2,
    "use_hardcoded_key": true
  }
}
```

配置项说明：

| 配置项 | 说明 |
|---|---|
| `provider` | 固定为 `baidu_segment` |
| `token_url` | 获取 access_token 的地址 |
| `segment_url` | 智能抠图接口地址 |
| `method` | 推荐 `auto` |
| `refine_mask` | 推荐 `true` |
| `return_form` | 推荐 `rgba` |
| `timeout_seconds` | 请求超时时间 |
| `retry_times` | 调用失败重试次数 |
| `use_hardcoded_key` | 演示项目可设置为 `true` |

---

## 9. 推荐目录结构

只针对百度云抠图部分，建议新增：

```text
app/
  baidu_segment_service.py     # 百度云智能抠图接口封装
  secrets.py                   # 短期演示项目可放置 AK / SK
```

---

## 10. secrets.py 示例

```python
# -*- coding: utf-8 -*-

# 仅适合短期演示项目。
# 不要提交到 Git，不要写入前端 HTML / JS。
# 演示结束后建议删除或禁用。

BAIDU_API_KEY = "请替换为你的百度云 API Key"
BAIDU_SECRET_KEY = "请替换为你的百度云 Secret Key"
```

---

## 11. baidu_segment_service.py 示例代码

下面是建议封装后的代码。

```python
# -*- coding: utf-8 -*-

import base64
import json
import os
import time
from pathlib import Path
from typing import Optional

import requests

try:
    from app.secrets import BAIDU_API_KEY, BAIDU_SECRET_KEY
except Exception:
    BAIDU_API_KEY = os.getenv("BAIDU_API_KEY")
    BAIDU_SECRET_KEY = os.getenv("BAIDU_SECRET_KEY")


class BaiduSegmentService:
    def __init__(self, config: dict):
        api_cfg = config.get("matting_api", {})

        self.api_key = BAIDU_API_KEY
        self.secret_key = BAIDU_SECRET_KEY

        if not self.api_key or not self.secret_key:
            raise RuntimeError(
                "百度云 API_KEY / SECRET_KEY 未配置，请检查 app/secrets.py "
                "或环境变量 BAIDU_API_KEY / BAIDU_SECRET_KEY"
            )

        self.token_url = api_cfg.get(
            "token_url",
            "https://aip.baidubce.com/oauth/2.0/token"
        )
        self.segment_url = api_cfg.get(
            "segment_url",
            "https://aip.baidubce.com/rest/2.0/image-process/v1/segment"
        )

        self.method = api_cfg.get("method", "auto")
        self.refine_mask = api_cfg.get("refine_mask", "true")
        self.return_form = api_cfg.get("return_form", "rgba")
        self.timeout = int(api_cfg.get("timeout_seconds", 60))

        self._access_token: Optional[str] = None
        self._token_expire_at: float = 0

    def get_access_token(self) -> str:
        # 简单 token 缓存，避免每次请求都重新获取 token。
        now = time.time()
        if self._access_token and now < self._token_expire_at:
            return self._access_token

        params = {
            "grant_type": "client_credentials",
            "client_id": self.api_key,
            "client_secret": self.secret_key
        }

        resp = requests.post(
            self.token_url,
            params=params,
            timeout=self.timeout
        )
        resp.raise_for_status()

        data = resp.json()

        access_token = data.get("access_token")
        if not access_token:
            raise RuntimeError(f"获取百度 access_token 失败：{data}")

        expires_in = int(data.get("expires_in", 2592000))
        self._access_token = access_token
        self._token_expire_at = now + expires_in - 60

        return access_token

    @staticmethod
    def image_to_base64(image_path: str) -> str:
        with open(image_path, "rb") as f:
            return base64.b64encode(f.read()).decode("utf-8")

    @staticmethod
    def save_base64_image(image_base64: str, output_path: str) -> str:
        image_bytes = base64.b64decode(image_base64)
        Path(output_path).parent.mkdir(parents=True, exist_ok=True)

        with open(output_path, "wb") as f:
            f.write(image_bytes)

        return output_path

    def segment_image(self, image_path: str, output_path: str) -> str:
        # 调用百度智能抠图接口，并保存结果图。
        access_token = self.get_access_token()
        url = f"{self.segment_url}?access_token={access_token}"

        image_base64 = self.image_to_base64(image_path)

        payload = {
            "image": image_base64,
            "method": self.method,
            "refine_mask": self.refine_mask,
            "return_form": self.return_form
        }

        headers = {
            "Content-Type": "application/json",
            "Accept": "application/json"
        }

        resp = requests.post(
            url,
            headers=headers,
            data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
            timeout=self.timeout
        )
        resp.raise_for_status()

        result = resp.json()

        if "error_code" in result:
            raise RuntimeError(f"百度智能抠图失败：{result}")

        result_image_base64 = result.get("image")
        if not result_image_base64:
            raise RuntimeError(f"百度智能抠图未返回 image 字段：{result}")

        return self.save_base64_image(result_image_base64, output_path)
```

---

## 12. 主流程示例

```python
def process_baidu_segment_flow(
    local_image_path: str,
    config: dict,
    cutout_save_path: str
) -> str:
    segment_service = BaiduSegmentService(config)

    cutout_path = segment_service.segment_image(
        image_path=local_image_path,
        output_path=cutout_save_path
    )

    return cutout_path
```

调用示例：

```python
config = {
    "matting_api": {
        "provider": "baidu_segment",
        "method": "auto",
        "refine_mask": "true",
        "return_form": "rgba",
        "timeout_seconds": 60
    }
}

cutout_path = process_baidu_segment_flow(
    local_image_path="static/captures/test.jpg",
    config=config,
    cutout_save_path="static/cutouts/test.png"
)
```

---

## 13. 使用 rgba 返回值进行背景合成

当 `return_form="rgba"` 时，保存下来的结果图是透明背景 PNG，可以直接合成到模板背景上。

```python
from PIL import Image


def paste_person_to_background(
    person_png_path: str,
    background_path: str,
    output_path: str
):
    bg = Image.open(background_path).convert("RGBA")
    person = Image.open(person_png_path).convert("RGBA")

    target_h = int(bg.height * 0.68)
    scale = target_h / person.height
    target_w = int(person.width * scale)

    person = person.resize((target_w, target_h), Image.LANCZOS)

    x = (bg.width - target_w) // 2
    y = bg.height - target_h - 120

    bg.paste(person, (x, y), person)
    bg.convert("RGB").save(output_path, quality=95)
```

---

## 14. 使用 mask 返回值时的处理方式

如果配置：

```python
return_form = "mask"
```

返回结果是单通道 mask 图，需要结合原图生成透明人物图。

```python
from PIL import Image


def build_rgba_from_mask(
    original_path: str,
    mask_path: str,
    output_path: str
) -> str:
    original = Image.open(original_path).convert("RGBA")
    mask = Image.open(mask_path).convert("L")

    if mask.size != original.size:
        mask = mask.resize(original.size)

    original.putalpha(mask)
    original.save(output_path)

    return output_path
```

第一版建议优先使用 `rgba`，避免额外处理 mask。

---

## 15. 与你当前示例代码的对比

你的示例代码：

```python
payload = json.dumps({
    "refine_mask": "true",
    "method": "auto"
}, ensure_ascii=False)
```

建议改为：

```python
image_base64 = base64.b64encode(open(image_path, "rb").read()).decode("utf-8")

payload = json.dumps({
    "image": image_base64,
    "refine_mask": "true",
    "method": "auto",
    "return_form": "rgba"
}, ensure_ascii=False)
```

---

## 16. 异常处理建议

| 异常场景 | 处理建议 |
|---|---|
| AK / SK 为空 | 启动时直接报错，提示检查 `app/secrets.py` |
| access_token 获取失败 | 打印百度返回 JSON，检查 AK / SK |
| 图片 base64 过大 | 降低图片分辨率或压缩图片 |
| 图片格式不支持 | 转换为 JPG / PNG |
| 接口返回 `error_code` | 记录完整返回内容 |
| 返回结果没有 `image` 字段 | 打印完整响应 |
| `return_form=mask` 但按 PNG 合成 | 检查配置 |
| 人物边缘效果差 | 保持 `refine_mask=true`，优化拍照背景和光线 |
| 调用耗时过长 | 降低输入图片尺寸或增加超时时间 |

---

## 17. 测试用例

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| B01 | 代码中直接配置 AK / SK | 能获取 access_token |
| B02 | AK / SK 错误 | 返回鉴权失败 |
| B03 | 本地图片转 base64 | 成功生成字符串 |
| B04 | 调用智能抠图接口 | 返回 JSON |
| B05 | `return_form=rgba` | 返回透明背景 PNG 的 base64 |
| B06 | 保存结果图 | 本地生成 PNG 文件 |
| B07 | 使用 PNG 换背景 | 人物透明区域正确 |
| B08 | `return_form=mask` | 返回单通道 mask |
| B09 | 图片过大 | 能明确报错或压缩后重试 |
| B10 | 无主体图片 | 返回失败或效果不佳提示 |

---

## 18. 最终建议

针对当前短期演示项目，建议采用：

```text
1. AK / SK 临时写入 app/secrets.py
2. 本地图片直接转 base64
3. 调用百度智能抠图接口
4. return_form 使用 rgba
5. refine_mask 使用 true
6. 接口返回 image 字段后，base64 解码保存为 PNG
7. 后续交给 Python 背景合成模块
```

关键注意点：

```text
不要把 AK / SK 写入前端 HTML / JavaScript；
不要提交到 Git；
演示结束后删除或禁用 AK / SK；
建议优先使用 return_form="rgba"。
```
